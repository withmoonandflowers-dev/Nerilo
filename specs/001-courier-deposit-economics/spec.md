# Spec 001：為盲信使寄存側建立經濟均衡

- 軌別：feature（quote/receipt 格式標記為未來 protocol 軌條目，見第 4 節）
- 狀態：planned
- 建立：2026-07-14／最後更新：2026-07-14（clarify 五題拍板，plan 填入）
- 關聯：ADR-0024（盲信使儲存經濟學）、ADR-0022（可驗證點數帳本）、風險登記 R5、**依賴 Spec 002（R5 收斂，前置）**

## 1. 要做什麼、為什麼（specify）

盲信使目前寄存免費，且寄存動作本身會刷新 LRU 熱度（CourierStore lastAccessedAt），攻擊者能以趨近零的成本持續寄存垃圾密文並保溫自己的房，把誠實房間擠出信使快取。因為信使是盲的（看不懂密文），內容過濾天生不可行，防線只能建立在出處與經濟上。本規格以價格理論建立寄存側經濟均衡：供給隨在線人數內生、需求付市價、點數守恆、貢獻是唯一的賺點途徑，使垃圾寄存從「零成本傷害他人」變成「先當生產者才能當消費者」，外部性內部化。

**憲法檢核**（constitution.md）：
- 目標函數加分項：隱私韌性（信使網路是斷網送達的地基）；補助競爭力（分散式價格均衡設計是技術深度敘事的一章）。
- 四條不變量影響：恰好一次〈無〉；E2EE〈無〉；帳本正當性〈有，依賴 Spec 002 先收 R5〉；身分授權〈有，per-身分配額綁非匿名反女巫〉。

## 2. 邊界（明確不做）

- 不做全域共識帳本／區塊鏈（維持 ADR-0011 觸發條件判準）。
- 不做鑄造銷毀制貨幣（等於重新發明央行）；採移轉制，點數守恆。
- 不做協議層中央定價；協議只定義市場原語（quote/receipt），定價留各節點自主。
- 不做檔案／媒體代存配額市場（ADR-0024 延後）。
- 不做內容審查（盲性是特性）。
- LRU 大修（淘汰權重乘貢獻分）不做，留觀察；只做小修（寄存不刷熱度）。

## 3. 待釐清（clarify）——已全數拍板 2026-07-14

- [x] Q1 計價單位：**byte×天**（storage）與 **byte**（serve/relay）。價格理論模型見第 4 節；使用者要求以供需均衡而非固定費。
- [x] Q2 冷啟動：**生產者優先**。新身分初始配額 ε（夠輕度使用、不夠攻擊），想要更多先貢獻中繼轉發與寄存代管，經共簽收據驗證入帳。
- [x] Q3 執行位置：**本地裁量定價 + 協議層定原語**（quote 報價、receipt 收據、拒收語義）。定價演算法不入協議。
- [x] Q4 LRU：**只做小修**——寄存不刷新 lastAccessedAt，僅成員 pull（真實使用）刷新。大修不做。
- [x] Q5 R5：**拆成 Spec 002 前置**，本 spec 依賴之。

## 4. 技術計畫（plan）

### 4.1 經濟模型（分散式擁擠定價，移轉制）

市場資源：寄存儲存（byte×天）、補齊頻寬（byte）、中繼轉發（byte）、訊號引導（次）。
總供給 S ≈ N（在線節點數）× 人均預算 × 在線率，隨參與人數內生；總需求 D 隨活躍房間與訊息量走。

個體定價（每信使自主，局部調價）：

```
p_j(t+1) = clamp( p_j(t) × [1 + λ(u_j(t) − u*)] , p_min , p_max )
u_j = 占用/預算；u* = 目標使用率（初值 0.7）；λ = 調整速度（初值 0.1/輪）
```

消費者選擇（寄存 q byte·天、冗餘 k 份）：

```
min Σ_{j∈K} p_j·q   s.t. |K| = k，總支出 ≤ 點數餘額 m_i
```

套利使活躍信使價格趨同（一價定律）；市場出清 Z(p*) = D(p*) − S(p*) = 0。
需求端天然價格上限 = 「成員互補（免費但慢）」的出走選擇權，市場不失控且免費底線服務保留（公共財性質）。

貨幣面（移轉制，守恆）：

```
Δm_i = w·(經共簽收據驗證的貢獻量) − Σ p·(消費量)；全網 Σ Δm_i = 0
```

冷啟動：m_i(0) = ε；ε 綁非匿名反女巫，女巫成本 = 開分身成本 × ε。

### 4.2 落地切面

- `CourierStore`：寄存不再刷新 lastAccessedAt（Q4 小修）；新增 per-寄存者（簽章身分）占用統計。
- `CourierService`：DEPOSIT 前檢查寄存者配額/餘額；新增 QUOTE 訊息（信使回報目前單價）；拒收語義（超配額/餘額不足）。
- `CreditLedger` + `CoSignedReceipt`：消費側扣點（寄存=付給信使）、貢獻側入點（沿用收據驗證；依賴 Spec 002 的型別強制）。
- 定價器：純函式模組（利於單元測試與模擬），信使側掛載。
- 協議原語標記：QUOTE 與 receipt 的線上格式，完成後開 protocol 軌 spec 記錄（供未來多實作互通）。

### 4.3 取捨（完成後回填 ADR）

- 移轉制 vs 鑄造銷毀：選移轉制，守恆免調控（理由如第 2 節）。
- 局部擁擠定價 vs 全域喊價：去中心化無喊價者，局部調價 + 套利收斂是可實作的等價物。
- ε 初始配額 vs 純零起步：純零起步會把「只想跟朋友聊天的輕使用者」也擋在門外，違反公共財定位。

## 5. 任務分解（tasks）

- [x] T0：等 Spec 002（R5 收斂）合併。〔2026-07-14 已落地，ADR-0028〕
- [ ] T1 ⚠：CourierStore 寄存不刷熱度 + per-身分占用統計（characterization-first，先釘現行 LRU 行為）。
- [ ] T2：定價器純函式模組（含收斂性單元測試：固定供需下價格收斂、震盪參數界）。
- [ ] T3 ⚠：CourierService QUOTE/扣點/拒收接線（P4 courier e2e 迴歸）。
- [ ] T4：冷啟動 ε 與貢獻入帳迴路接線（收據→餘額→可寄存）。
- [ ] T5：攻擊模擬（記憶體多節點）：垃圾寄存者 vs 誠實房間，驗證不被擠出。
- [ ] T6：回填 ADR + 開 protocol 軌 spec 記錄 QUOTE/receipt 格式。

## 6. 驗收（黃金判準）

- [ ] V1（攻擊模擬）：單一攻擊身分持續寄存，誠實房間不被擠出快取（對照現況會被擠出）。
- [ ] V2（經濟迴路）：無貢獻史身分耗盡 ε 後被拒收；完成中繼貢獻（共簽收據）後配額恢復。
- [ ] V3（價格收斂）：模擬固定供需下，各信使價格收斂到同一水準（一價定律），高負載漲價、低負載降價。
- [ ] V4（出走選擇權）：信使全滿/全拒時，訊息仍經成員互補最終送達（免費底線不破）。
- [ ] V5（迴歸）：P4 courier 既有 e2e（deposit/pull/tombstone/receipt）全綠不變。

## 7. 一致性自查（analyze，implement 前跑一次）

- [ ] 第 4 節方案覆蓋第 1 節全部需求，無多做
- [ ] 第 5 節任務完整實現第 4 節，無遺漏
- [ ] 第 6 節驗收能證明第 1 節，不是只證明「程式跑得動」
- [ ] 未違反憲法任何一條
