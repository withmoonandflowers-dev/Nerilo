/**
 * Ports：應用層依賴的介面，實作由 Adapters 注入。
 */
export type { IRoomService } from './IRoomService';
export type { IChatStorage } from './IChatStorage';
export type { IRoomDirectory, RoomSnapshot, DirectoryIdentity } from './IRoomDirectory';
